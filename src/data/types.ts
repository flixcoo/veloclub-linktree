import type {IconType} from 'react-icons';

export type TreeLink = {
    title: string;
    url: string;
    image: string;
};

export type SocialLink = {
    title: string;
    url: string;
    icon: IconType;
};
